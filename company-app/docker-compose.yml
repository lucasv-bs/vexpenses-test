version: '3.10'
services:
  company-app:
    build:
      args:
        user: lucas
        uid: 1000
      context: ./
      dockerfile: Dockerfile
    container_name: company-app
    depends_on:
      - company-app-db
    image: vexpenses-test-company-app
    networks:
      - company-app-net
    ports:
      - '8003:80'
    restart: unless-stopped
    volumes:
      - ./:/var/www/html/

  company-app-db:
    image: mysql
    container_name: company-app-db
    networks:
      - company-app-net
    ports:
      - '33063:3306'
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql

networks:
  company-app-net:
    driver: bridge